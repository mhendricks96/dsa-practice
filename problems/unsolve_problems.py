# given a tree, sort the values iterively
# given a linked list, return a list of all duplicate values
#given a linked list, find the maximum value recursively
#given a stack, sort the values iterively
# given a binary tree, find the maximum value recursively
# given a stack, find the sum of all elements iterively